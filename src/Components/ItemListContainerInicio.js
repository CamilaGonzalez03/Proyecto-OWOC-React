import React from 'react'

const ItemListContainerInicio = ( ) => {
    const styles = {
        color: '#CA431D',
        fontFamily: 'cursive',
        fontSize: '20px',
        paddingTop: '20px',
        textAlign: 'center',
    }
    
    return (
        
        <div style={styles}>Venta de frutos secos, semillas, granolas y más!!</div>
    )
}

export default ItemListContainerInicio